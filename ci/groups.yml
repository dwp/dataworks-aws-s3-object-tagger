groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
      - preprod
      - production
  
  - name: pull-request
    jobs:
      - dataworks-aws-s3-object-tagger-pr
  
  - name: update-pipeline
    jobs:
      - update-pipeline
